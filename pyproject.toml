[project]
name = "batch_renamer"
version = "0.1.0"
description = "Утилита для пакетного переименования файлов"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
batch-renamer = "batch_renamer.__main__:main"

[tool.pytest.ini_options]
pythonpath = ["."]