# 📁 Batch Renamer (Утилита пакетного переименовывания файлов)

Утилита командной строки для пакетного переименования файлов с гибкими шаблонами. Поддерживает режим «сухого» прогона (dry‑run), подстановку счётчика. Написана на Python с использованием **только стандартной библиотеки**, кроме тестов, для них используется фреймворк `pytest`.

## ✨ Возможности

- Переименование всех файлов в указанной папке по шаблону
- Указание целевой папки через `--path` (относительный или абсолютный путь)
- Поддержка подстановки:
  - `{counter}` – порядковый номер (с форматированием, например `{counter:03d}`)
- Режим `--dry-run` – предварительный просмотр без фактического переименования
- Указание расширений, для которых будет произведена замена через `--ext` (если расширений несколько, 
указывать через пробел).
- Сохранение исходного расширения (`{ext}`)
- Защита от конфликтов имён (пропуск файла с конфликтным новым именем и вывод предупреждения на экран)
- Кроссплатформенность (Linux, macOS, Windows)

## 📁 Структура проекта

```text
batch-renamer/
├── batch_renamer/                          # Основной пакет
│   ├── cli/                                # Подпакет для работы с командной строкой
│   │   ├── __init__.py
│   │   ├── get_args.py                     # Парсинг аргументов (argparse)
│   │   └── messages.py                     # Вывод сообщений, предупреждений, запросов
│   ├── __init__.py
│   ├── __main__.py                         # Точка входа (python -m batch_renamer)
│   ├── exceptions.py                       # Пользовательские исключения
│   ├── renamer.py                          # Класс Renamer (основная логика)
│   └── validators.py                       # Валидация путей, имён, параметров счётчика
├── tests/                                  # Тесты
│   ├── cli/                                # Тесты для подпакета cli
│   │   ├── test_get_args.py
│   │   ├── test_get_changes_accept.py
│   │   ├── test_show_changes.py
│   │   ├── test_show_empty_message.py
│   │   ├── test_show_error.py
│   │   ├── test_show_success_changes.py
│   │   └── test_show_warning.py
│   ├── validators/                         # Тесты для validators.py
│   │   ├── test_validate_counter_param.py
│   │   ├── test_validate_filename.py
│   │   └── test_validate_path.py
│   └── test_renamer.py                     # тесты для Renamer
├── .gitignore
├── pyproject.toml                          # Конфигурация проекта, зависимости, pytest
└── README.md
```

## ⚙️ Установка и запуск

### Требования

- Python 3.10 или выше (стандартная библиотека)

### 1. Клонирование репозитория

```bash
git clone https://github.com/CondoR-R/batch_renamer.git
cd batch_renamer
```

### 2. Создание виртуального окружения (рекомендуется)

Установка позволяет запускать утилиту командой batch-renamer из любого места.

```bash
# Linux / macOS
python3 -m venv venv
source venv/bin/activate

# Windows
python -m venv venv
venv\Scripts\activate

# Установите пакет в режиме разработки (editable)
pip install -e .
```

После установки утилита доступна как `batch-renamer`:

```bash
batch-renamer --pattern "photo_{counter:03d}.jpg" --dry-run
```

### 3. Запуск без установки (из исходников)

Если вы не хотите устанавливать пакет, можно запустить скрипт напрямую:

```bash
python -m batch_renamer [опции]
```

Пример:

```bash
python -m batch_renamer --pattern "photo_{counter:03d}.png" --dry-run
```

## 🚀 Использование

### Аргументы командной строки

| Аргумент | Описание |
|----------|----------|
| `--path` | Папка с файлами (по умолчанию текущая) |
| `--pattern` | Шаблон имени (обязательный) |
| `--dry-run` | Только показать, что будет переименовано, без изменений |
| `--ext` | Указать расширения изменяемых файлов |
| `--help` | Показать справку |

### Примеры

#### Простой шаблон со счётчиком

```bash
batch-renamer --pattern "photo_{counter:03d}.{ext}"
```

Результат: `photo_001.png`, `photo_002.png`, … (сортировка по имени файла).

#### Переинование файлов с указанными расширениями (без указания типа файла в паттерне)

```bash
batch-renamer --pattern "photo_{counter:03d} --ext jpg png"
```

Результат: `photo_001.png`, `photo_002.jpg`, `photo_003.png`, ... (сортировка по имени файла).

#### Переинование файлов с указанными расширениями (с указанием типа файла в паттерне)

```bash
batch-renamer --pattern "photo_{counter:03d}.{ext} --ext jpg png"
```

Результат: `photo_001.png`, `photo_002.jpg`, `photo_003.png`, ... (сортировка по имени файла).


#### Режим сухого прогона

```bash
batch-renamer --pattern "new_{counter:03d}.png" --dry-run
```

Выведет таблицу старых и новых имён, ничего не меняя.

## 🧪 Тестирование

Для тестирования используется фреймворк **pytest** (не входит в стандартную библиотеку, поэтому его нужно установить отдельно).

### Установка зависимостей для разработки

```bash
pip install pytest
```

#### Запуск тестов

```bash
pytest
```

Или с подробным выводом:

```bash
pytest -v
```

Тесты находятся в директории `tests/`. 

## 📄 Лицензия

MIT License. Подробнее см. в файле [LICENSE](LICENSE).

## 👤 Автор

[CondoR-R](https://github.com/CondoR-R)
